export interface SalamProps {
  description: string;
}

export interface InputProps {
  id: string;
  cellNo: string;
  content: string;
  inputType: string;
  hasLabel: boolean;
  inputName: string;
  componentType: string;
  order: number;
}